import biuoop.GUI;
import biuoop.DrawSurface;

import javax.swing.*;
import java.awt.*;
import java.util.ArrayList;
import java.util.List;

public class Main {

        public static void main(String[] args) {

            /*JFrame frame = new JFrame("catan");
            frame.setSize(2000, 1200);
            JPanel panel = new Board();
            SettlementPanel settlementPanel = new SettlementPanel();
            settlementPanel.addMouseListener(new SettlementClickListener(settlementPanel));
            frame.setContentPane(panel);
            frame.setContentPane(settlementPanel);
            frame.setDefaultCloseOperation(JFrame.DISPOSE_ON_CLOSE);
            frame.setVisible(true);

             */

        }
    }

